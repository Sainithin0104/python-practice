numbers = [1, 2, 2, 3, 1, 4, 2]

checked = []

for num in numbers:
    if num not in checked:
        count = 0

        for item in numbers:
            if item == num:
                count += 1

        print(num, "occurs", count, "times")
        checked.append(num)
